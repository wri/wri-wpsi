dist: xenial
language: ruby
cache:
  bundler: true
  directories:
    - vendor/bundle
    - node_modules
    - /home/travis/.rvm/
    - /home/.cache/yarn

services:
  - postgresql

addons:
  chrome: stable

script:
  - bin/ci-build
